```javascript
做一个“AI 会议纪要任务提取器”
在真实团队协作中，会议纪要通常是大段自然语言。真正有价值的信息包括：
- 要做什么
- 谁负责
- 截止时间
- 风险和阻塞项
请你实现一个本地可运行的应用，把一段原始会议纪要转成结构化任务，并完成最小闭环。

二、任务要求
请至少完成以下主链路：
1. 输入或粘贴一段会议纪要文本
2. 点击一个按钮触发“提取”
3. 展示结构化任务结果
4. 至少支持编辑一条结果
5. 至少支持保存一次结果
6. 能再次看到已保存的数据
结构化结果至少包含
- title 或任务内容
- owner 负责人
- deadline 截止时间
- risk 或风险说明
status、priority、summary、sourceText 等字段为可选加分项。

三、范围建议
前端：
1. 一个文本输入区
2. 一个“开始提取”按钮
3. 一个任务列表展示区
4. 一个编辑入口
5. 一个“保存”按钮
6. 一个已保存结果展示区
后端：
1. POST /extract
2. POST /save
3. GET /records
4. 真正真实模型（线上/本地模型均可）
5. 禁止mock，需接入真实数据库
```